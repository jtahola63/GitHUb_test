# GutHub_test
mkdir <dir>
cd <dir>
git init
copy con README ... lisää tesktiä ja paina <CTRL><Z>
git add .
git commit -m "First commit"
Chrome => GitHub => Repositories
New repository
copy <URL>
git remote add main <URL>
git remote -v
git push <GitHub branch> <local branch>
