# GutHub_test
mkdir <dir>
cd <dir>
git init
copy con README ... lisää tekstiä ja paina <CTRL><Z> tai esim notepadissa
git add .
git commit -m "First commit"
Chrome => GitHub => Repositories
New repository
copy <URL>
git remote add main <URL>
git remote -v
get status => show what is your local branch name
git push <GitHub branch> <local branch>
